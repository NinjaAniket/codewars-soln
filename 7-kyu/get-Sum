function getSum(a, b) {
  if (a == b) return a;
  let sum = 0;

  const maxItem = Math.max(a, b);
  const minItem = Math.min(a, b);
  for (let i = minItem; i <= maxItem; i++) {
    sum += i;
  }
  return sum;
}

console.log(getSum(-1, 2));
